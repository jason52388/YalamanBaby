# Serves the static site over plain HTTP on :80.
# HTTPS and domain routing are handled by your caddy-docker-proxy in front.
:80 {
	root * /srv
	file_server
	try_files {path} /index.html
	encode gzip
}
