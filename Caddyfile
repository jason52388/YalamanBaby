# {$SITE_DOMAIN} is supplied at runtime (from docker-compose environment).
# Caddy automatically provisions and renews a free HTTPS certificate for it.
{$SITE_DOMAIN} {
	root * /srv
	file_server
	try_files {path} /index.html
	encode gzip
}
